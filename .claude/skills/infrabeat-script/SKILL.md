---
name: infrabeat-script
description: "Use this skill when the user asks to add or modify server scripts, client scripts, or hooks for custom_erp. Triggers include 'add server script', 'add client script', 'modify hooks.py', 'add validation', 'add scheduled job', 'add custom button', or naming a hook event (validate, before_save, on_submit, etc.). Produces Python controller methods, JS client scripts, or hooks.py entries and writes them to the appropriate paths in custom_erp for VS Code review. Do NOT use for new DocType creation (use infrabeat-doctype) or for database schema changes."
---

# infrabeat-script

> **STATUS:** Stub. Implementation pending in Phase 3.
> Frontmatter is valid (name + description) so Claude Code skill discovery picks it up.
> Body will be filled with: When invoked / Inputs / Hard rules / Steps / Verification.